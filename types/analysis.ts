export interface Analysis {
    average_price: string;     // 平均价格
    average_volume: string;    // 平均成交量
    average_turnover: string;  // 平均成交额
    total_volume: string;      // 总成交量
    total_turnover: string;    // 总成交额
    highest_price: string;     // 最高价
    lowest_price: string;      // 最低价
}
